import React from 'react';
import { shallow } from 'enzyme';

describe('displayVehicles, fetchVehicles, cleanVehicles', () => {
  it('should redirect to /vehicles if there are vehicles in the store', () => {

  })

  it('should call fetchVehicles if there are no vehicles in the store', () => {

  })

  it('should call fetchAnything with the correct url', () => {

  })

  it('should call cleanVehicles with the correct data', () => {

  })

  it('should return an array of objects with the correct keys when cleanVehicles is called', () => {

  })

  it('should call storeVehicles when cleanVehicles is called', () => {
    
  })


})